version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stuanimelist_app
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_KEY= base64:rSYDkfV9z23p9rZOog/vwpiX7Ij3yQMqAO47ExWp0xA=
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/database/database.sqlite 
    volumes:
      - stuanimelist_db:/var/www/database

volumes:
  stuanimelist_db: